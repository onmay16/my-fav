[uwsgi]
chdir = /srv/myfav/backend/
module = backend.wsgi:application
home = /home/ubuntu/env/

uid = ubuntu
gid = ubuntu

http = :8080

enable-threads = true
master = true
vacuum = true
pidfile = /tmp/myfav.pid
logto = /var/log/uwsgi/myfav/@(exec://date +%%Y-%%m-%%d).log
log-reopen = true